uses "sys.h"
uses "math.h"

label main
 push 30
 invoke sin
 invoke coutln
 push 30
 invoke cos
 invoke coutln
 push 30
 invoke tg
 invoke coutln
 push 30
 invoke ctg
 invoke coutln
return